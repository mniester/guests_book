1) the app does not work without import routes to run.py
2) forms do not send feedback (errors) (it's HTML error)
3) flashes do not work
4) add option to render other quantity of posts than default
5) API received bad response*
6) Change post form to name to not misleading (entry)*
7) AJAX, JQuery (optional)
8) After sending query, entry is also validated*

*solved
